name: Research Phase 02 - Features
on:
  workflow_dispatch:
    inputs:
      research_branch:
        description: Phase branch
        required: true
        default: research/phase-02-features
        type: string
permissions:
  contents: write
jobs:
  features:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.research_branch }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: |
          python -m pip install -U pip
          pip install -r requirements.txt
          pip install -r requirements-research.txt
      - name: Preflight
        run: python scripts/research/preflight.py --phase phase-02-features --branch "${{ inputs.research_branch }}"
      - name: Execute phase
        run: python scripts/research/phase_runner.py --phase phase-02-features
      - name: Record and push
        run: |
          python scripts/research/record_step.py --phase phase-02-features --step feature_phase --status READY --summary "Feature phase scaffold executed; final feature table requires validated Phase 01 data."
          python scripts/research/build_pages.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/STATUS.json research/RUN_LOG.jsonl docs/ data/
          git diff --cached --quiet || (git commit -m "Research: phase 02 feature status" && git push origin "${{ inputs.research_branch }}")
